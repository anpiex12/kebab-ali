# golangci-lint v2 configuration. The default ("standard") linter set —
# errcheck, govet, ineffassign, staticcheck and unused — is enough to keep this
# codebase honest without drowning it in style noise.
version: "2"

run:
  timeout: 5m

linters:
  default: standard
